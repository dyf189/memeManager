# .mpak 导出格式规范（讨论稿 v0.1）

> 本文件是 Android / Windows / Linux 三端实现 `.mpak` 格式的**唯一事实来源**。
> 任何格式变更必须先改本文件，再改代码；格式冻结后只能升版本号，禁止原地修改旧版本。
>
> 状态：**讨论中，未定稿**。标注 `[待定]` 的条目需要三端协商后拍板。

---

## 1. 文件总览

`.mpak`（Meme Package）是自包含的单文件分片：一个文件 = 一份完整可独立导入的媒体集合。

```
┌──────────────────────────────────────────────┐
│ 文件头（14 字节，固定）                        │
│ 元数据 JSON 段（UTF-8，变长）                  │
│ 媒体数据段（多个文件，变长）                    │
│ SHA-256 哈希（32 字节，固定）                  │
└──────────────────────────────────────────────┘
```

---

## 2. 二进制布局（字节级定义）

### 2.1 文件头（14 字节）

| 偏移 | 长度 | 字段 | 值 | 说明 |
|------|------|------|-----|------|
| 0 | 4B | 魔数 | `0x4D 0x50 0x41 0x4B`（"MPAK"） | 文件类型指纹 |
| 4 | 2B | 版本号 | `0x0001` | 格式版本，升版递增 |
| 6 | 4B | 媒体数量 | 无符号整数 | JSON 段里的 media 条目数 |
| 10 | 4B | 元数据 JSON 字节长度 | 无符号整数 | 不含任何 padding |

### 2.2 元数据 JSON 段

从偏移 14 开始，长度 = 文件头第 10 字节处的值。UTF-8 编码，schema 见第 3 节。

### 2.3 媒体数据段

紧接 JSON 段之后，**媒体数量条**记录，每条：

| 长度 | 字段 | 说明 |
|------|------|------|
| 2B | 文件名长度 | 无符号整数 |
| 变长 | 文件名 | UTF-8 编码，见第 5.2 节命名规则 |
| 8B | 文件内容长度 | 无符号整数 |
| 变长 | 文件二进制内容 | 原样字节 |

**顺序约定**：媒体数据段的顺序必须与 JSON 段 `media` 数组的顺序一致（第 i 条记录对应 `media[i]`，靠文件名二次校验，见 4.3）。

### 2.4 尾部哈希

最后 32 字节 = **整个文件除这 32 字节外的全部内容**的 SHA-256。

> ⚠️ 覆盖范围是"文件头 + JSON 段 + 媒体数据段"全部，导出和导入两端必须用同一口径。

### 2.5 字节序（重要！）

**全文件所有多字节整数一律使用大端（Big-Endian，网络字节序）。**

- 魔数 4B、版本 2B、媒体数量 4B、JSON 长度 4B、文件名长度 2B、内容长度 8B —— 全部大端
- Android/Java 用 `DataOutputStream`（默认大端）即可
- Win/Linux 的 C/C++ 必须显式转换（`htobe32` / `htobe64` 或手动移位），**禁止直接 memcpy 结构体**（编译器会加 padding，且默认小端）

---

## 3. 元数据 JSON Schema

> `[待定]` 项见文末决策清单。

```jsonc
{
  "format": "memeManager",            // 固定标识
  "version": 1,                        // schema 版本（与二进制头版本号一致）
  "exportedAt": 1786000000000,         // 导出时间，Unix 毫秒时间戳（UTC）
  "media": [
    {
      "fileName": "meme_0001.jpg",     // 二进制段文件名（关联键，ASCII 安全名）
      "name": "搞笑猫猫图.jpg",         // 显示名（原始名，可为中文）
      "type": "IMAGE",                 // IMAGE | VIDEO | GIF
      "size": 102400,                  // 文件字节数（与二进制段内容长度一致）
      "sha256": "a1b2c3d4...",         // 媒体文件二进制内容的 SHA-256（十六进制小写），必填
      "width": 1080,                   // 可空 null
      "height": 1920,                  // 可空 null
      "description": "卡了",           // 可空 null
      "createdAt": 1785000001000,      // 入库时间，Unix 毫秒时间戳，必填
      "tags": ["搞笑", "猫"]           // 标签名数组，可为空 []
    }
  ]
}
```

### 3.1 字段约束

| 字段 | 必填 | 约束 |
|------|------|------|
| `format` | ✅ | 固定 `"memeManager"`，导入端校验，不匹配直接拒绝 |
| `version` | ✅ | 与二进制头版本一致 |
| `exportedAt` | ✅ | 毫秒时间戳 |
| `media[].fileName` | ✅ | 见 5.2 命名规则 |
| `media[].name` | ✅ | 显示名 |
| `media[].type` | ✅ | 三态枚举，字符串 |
| `media[].size` | ✅ | 必须等于二进制段实际内容长度 |
| `media[].sha256` | ✅ | 媒体文件二进制内容的 SHA-256，十六进制小写 |
| `media[].width/height` | ❌ | 未解析时为 `null`                                |
| `media[].description` | ❌ | `null` 或字符串 |
| `media[].createdAt` | ✅ | 毫秒时间戳 |
| `media[].tags` | ✅ | 数组，可为空；元素为标签名字符串 |

### 3.2 明确**不导出**的字段（本地概念）

| 字段 | 原因 |
|------|------|
| `id` | 本地自增主键，跨平台无意义 |
| `filePath` | 路径由导入端本地派生 |
| `storageType` | 导入落位由导入端策略决定（见 4.3 / 决策 D7） |
| `updatedAt` | 当前实现恒等于 createdAt（死字段） |
| `isDeleted` / `deletedTime` | 回收站是各端本地概念 |
| `exportedHash` | 本地字段，语义待与 JSON `sha256` 对齐（建议：`sha256` 即媒体文件二进制哈希，本地 `exportedHash` 直接存它，导入时用于内容去重） |

---

## 4. 导入流程

### 4.1 校验

1. 读前 4 字节，校验魔数 = "MPAK" → 否则拒绝
2. 读版本号，不支持的版本 → 拒绝并提示"版本过新/过旧"
3. 读末尾 32 字节为 `originalHash`
4. 对文件除末尾 32 字节外全部内容计算 SHA-256 → `calculatedHash`
5. 两者一致 → 继续；不一致 → 提示"文件已损坏"，跳过该分片

### 4.2 解析

1. 读偏移 6 的媒体数量、偏移 10 的 JSON 长度
2. 解析 JSON 段，校验 `format`、`version`
3. 按媒体数量逐条读二进制段：文件名长度 → 文件名 → 内容长度 → 内容
4. 三层校验（逐条）：
   - **定界安全**：读内容前，先比对 `declaredLen`（内容长度字段）与文件剩余字节数（`文件总长 - 当前偏移 - 32`），`declaredLen > 剩余字节` → 报"文件损坏"，跳过（防恶意/损坏长度值导致 OOM）
   - **文件名交叉验证**：`fileName` 与 JSON `media[i].fileName` 一致，不一致 → 报"文件损坏"（防错位）
   - **size 交叉验证**：`declaredLen` 与 JSON `media[i].size` 一致，不一致 → 报"文件损坏"（防导出端错位 bug——尾部哈希验不出这种错误）

### 4.3 提取与落位

1. 文件写入**导入端自己的媒体目录**（Android：设置页用户选择的位置；Win/Linux：各端配置目录）
2. 写入文件名：优先使用 `name` 的原始名；若与现有文件冲突或非法 → 自动加后缀（`_1`、`_2`…）或清洗（见 5.3）

### 4.4 去重（跨平台场景）

- "路径严格"策略在跨平台无意义（导入端永远是新路径）
- 默认策略：**大小 + 内容哈希**——与库中已有媒体比对 `size` 与 `sha256`（导入端对提取出的内容计算一次 SHA-256，与 JSON 里的 `sha256` 比对确认一致，再与库中记录比对），完全一致则跳过
- 内容哈希比对同时充当导出端到导入端的内容完整性校验

### 4.5 标签合并

- 标签以**字符串名**合并：同名复用已有标签，不存在则创建
- `[待定]` 保留标签（`isReserved`）是各端内部概念，JSON 不携带；`[已导出]` 由各端导入逻辑自行处理

### 4.6 错误处理

- 单文件提取失败不影响同分片其他文件
- 完成后显示摘要：成功 X 张，跳过 Y 张，失败 Z 张
- 失败哪几张弹窗显示

---

## 5. 编码与命名约定

### 5.1 文本编码

- JSON 段、文件名：一律 **UTF-8**
- **JSON 内容尽量写明文 UTF-8，不使用 `\uXXXX` 转义 emoji/非 BMP 字符**（三端解析器对代理对处理不一致）
- 仅标准转义：`\"` `\\` `\n` `\r` `\t` 等

### 5.2 二进制段文件名规则（导出端）

- 仅使用 ASCII 安全字符：`[a-zA-Z0-9._-]`
- 命名格式：`meme_` + 4 位序号 + 扩展名，如 `meme_0001.jpg`
- 扩展名按 type 映射：IMAGE → `.jpg`/`.png`（原扩展名保留但需清洗）、GIF → `.gif`、VIDEO → `.mp4` 等
- 不包含路径分隔符、不包含空格、不以 `.` 开头

### 5.3 导入端文件名清洗（Win 端必须）

Windows 禁止的字符：`\ / : * ? " < > |`
Windows 保留名（含扩展名也不允许）：`CON PRN AUX NUL COM1-9 LPT1-9`

清洗规则（示例）：
- 非法字符 → 替换为 `_`
- 保留名 → 前缀 `_`（`CON.jpg` → `_CON.jpg`）
- 末尾空格/点 → 去掉
- 长度 > 255（不含扩展名 260 总长）→ 截断 + 保留扩展名

---

## 6. 分片与打包

### 6.1 分片算法

**输入**：待导出媒体列表（时间降序）、`MAX_SIZE`（字节，默认 100MB）

**每片大小计算（三端必须同一公式）**：

```
片大小 = 14（文件头）
       + JSON 段字节长度
       + Σ（2 + fileName 字节长度 + 8 + 文件字节数）
```

**贪心逻辑**：

1. 遍历媒体，维护 `currentFiles`、`usedSize`
2. 计算 `newSize = usedSize + 2 + fileNameLen + 8 + fileSize`
3. `newSize ≤ MAX_SIZE` → 加入当前片
4. 当前片为空且 `fileSize > MAX_SIZE` → 单独成片（允许超限）
5. 否则 → 写当前片，开新片

**注意**：JSON 长度随媒体数增长，实际写入的片大小可能略超 `MAX_SIZE`——这是预期行为，三端一致即可。

### 6.2 空导出

- 0 个媒体 → 不生成文件，提示"未选择"

---

## 7. 决策清单（待三端协商）

| 编号 | 问题 | 建议 | 状态 |
|------|------|------|------|
| D1 | 字节序 | 全文件大端 | 是 |
| D2 | 标签表示 | 字符串名，同名合并 | 是 |
| D3 | 时间字段 | 只带 takenTime + createdAt，毫秒时间戳 | 删掉takenTime |
| D4 | source 字段 | 保留为可空字段；Android 端补赋值（"相册"/"文件夹"/"分享"） | 删掉 |
| D5 | 媒体级哈希 | JSON 加 `sha256` 字段（媒体文件二进制哈希），供导入内容去重 + 内容完整性校验 | ✅ 是 |
| D6 | 导入落位 | 各端本地目录（Android：设置页用户选择的位置），不随文件传输 | ✅ 是 |
| D7 | storageType | 不导出，导入端按自身策略 | ✅ 是 |
| D8 | 去重策略 | 跨平台默认"大小 + 内容哈希" | ✅ 是 |
| D9 | 导入是否覆盖已有媒体 | 弹窗提醒，让用户决定 | ✅ 是 |
| D10 | 版本演进 | 冻结后只升版；文档先行 | ✅ 是 |

---

## 8. 变更记录

| 版本 | 日期 | 变更 | 作者 |
|------|------|------|------|
| v0.1 | - | 初稿：基于 goals.md §5/§6 整理 + 跨平台冲突点补充 | - |
