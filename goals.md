# 表情包管理器 (memeManager) 开发规划文档

## 一、功能审核与完善（最终功能列表）

### 1. 媒体管理核心
- 支持格式：图片、GIF、视频（统称“媒体”）。可在设置中限制显示类型。
- 时间分组（相册主视图）：
  - 刚刚（<1分钟）、X分钟前、X小时前
  - 昨天、前天、三天前（以自然日零点为界）
  - 4天前至今年内：`MM-dd`
  - 去年及更早：`yyyy-MM-dd`
- 分组依据设置项：拍摄日期（优先读取 EXIF/MediaStore）或导入日期。
- 视图模式：网格/列表，可切换。默认时间降序排列，此顺序也为导出时的打包顺序。

### 2. 存储策略（三种模式共存）
每张图片可独立标记其存储类型，允许多种模式共存。

| 存储类型 | 说明 | 特点 |
|----------|------|------|
| 私有内部 | 文件存储在应用私有目录，与系统隔离。 | 数据安全，随应用卸载删除。 |
| 公共目录 | 在 `/storage/emulated/0/EmojiManager/` 等公共目录管理。 | 方便手动备份、跨应用访问。 |
| 外部索引 | 不复制文件，仅索引用户指定的外部目录，保持原位置。 | 不改动文件，适合临时管理。 |

- 导入 `.emp` 分片时，媒体文件统一放入公共目录（存储类型标记为公共目录）。
- 从外部索引转入其他类型时，文件复制到对应目标目录，原路径不再关联（不可逆）。
- 可在设置中统一查看各类型存储占用情况。
- 第三种模式可以转换为第一或第二种模式，但是不能再次转换回去

### 3. 数据与配置存储

- 主存储：Room 数据库 + FTS5 全文搜索（标签、描述、文件名）。

- 备份友好选项：可在设置中开启“同步维护 JSON 数据文件”，放在公共目录下，便于手动查看和备份。

- 导出格式：所有元数据使用 JSON（见导出章节）。

- ### 4. 添加与导入
  
- 添加途径：从系统相册/文件管理器选择、批量导入文件夹、接收系统分享。

- 添加时可操作：批量或逐个重命名、添加标签、设置来源（自动推断或手动选择）。

- 批量导入文件夹时提供“包含子目录”开关。

- 模式三下添加即为选择并索引，仅记录路径不复制文件。

- 去重策略（可在设置中配置）：
  - 路径严格：目标路径已存在则跳过。
  - 文件名+大小：同文件名且大小相同则跳过。
  - 感知哈希提醒（后台任务）：启用后，在后台计算图片的感知哈希，通过汉明距离判断视觉相似性。
    - 相似度阈值：用户可设置汉明距离阈值（默认5，范围1-10），距离 ≤ 阈值视为重复。
    - 发现重复时不自动删除，仅生成通知，用户可在“重复清理”界面手动处理。

### 5. 标签系统
- 标签管理器（独立界面）：增删改标签，自定义标签背景颜色，文字颜色根据背景亮度自动从黑/白里选对比度高的颜色。
- 标签名唯一，不允许重名。
- 预设背景色循环分配：内置一组默认色彩，新建标签时自动按顺序循环使用，用户可修改。
- 保留标签 `[已导出]`：不可创建同名、不可删除该标签种类，样式可编辑。
- 标签显示方式：
  - 相册顶部筛选栏：所有标签以带背景色的胶囊形状横向滑动，点选筛选。
  - 缩略图右下角：显示最多4个实心彩色圆点（标签背景色），超出显示灰色“+N”。
- 给媒体打标签：在详情页或多选批量操作中勾选。

### 6. 搜索与描述
- 每项媒体可编辑纯文本描述。
- 搜索功能：
  - 搜索范围：文件名、描述、标签名。
  - 模糊搜索：使用 FTS5 + 结巴分词（中文），编辑距离二次排序。
  - 搜索结果以列表展示，每条结果包含：
    - 左侧 48dp 缩略图
    - 右侧文字区：
      - 标题行：单行，14sp，默认文字色。若标题超过一行最大字符数，智能截断并优先在可见窗口内展示匹配内容，匹配内容加粗高亮，边缘适当添加省略号。
      - 描述行：最多两行，12sp，灰色。匹配内容加粗高亮，两行内智能截断，优先保证第一行完整及匹配区间的可见性。
      - 信息行：显示文件大小与日期，11sp，浅灰色，不参与高亮。
  - 截断与高亮的具体规则见第三步算法章节。
- 全局搜索栏，实时展示结果。

### 7. 筛选器（可叠加条件）
- 媒体类型（图片/GIF/视频/全部）
- 来源（相机、截图、微信、QQ、下载、自定义…）
- 带描述/无描述
- 包含选定标签（多选，AND 逻辑）
- 导出状态（已导出/未导出）
- 时间范围（自定义起止日期）
- 文件大小区间

### 8. 删除与回收站
- 删除机制：删除媒体先进入回收站，保留数据及标签，可设定保留天数（0=直接删除不进回收站）。
- 回收站可彻底删除或还原。
- 外部索引的图片彻底删除时仅移除数据库记录，可选同时删除原文件（二次确认）。
- 定期任务（WorkManager）：清理超过保留天数的回收站项目。

### 9. 导出与导入（.emp 格式）
- 导出格式：每个分片为一个自包含的单文件 `.emp`。
  - 内部结构：文件头（魔数、版本、媒体数量、元数据长度）、元数据 JSON 段、媒体数据段（每个文件带文件名、大小、二进制内容）、尾部 SHA-256 哈希。
- 导出顺序：严格按当前相册排列（时间降序），若使用筛选则按筛选结果顺序，若导出选定文件则按选定顺序
- 分片策略：用户设定大小上限，贪心填充，保证整数个媒体；单文件超过上限则单独成片。
- 支持“导出选中项”和“全部导出”，带进度条。
- 导出成功自动为所有导出媒体添加 `[已导出]` 标签。
- 导入分片：选取 `.emp` 文件，校验哈希后提取媒体至公共目录，元数据合并入库，并主动移除 `[已导出]` 标签。
- 一键操作（均需弹窗确认）：
  - “删除所有已导出媒体”（进回收站）
  - “从所有媒体移除[已导出]标签”

### 10. 分享转发

- 统一使用系统分享 Intent，支持单张或多张。
- 根据存储类型智能获取 URI：
  - 内部存储/公共目录：通过 FileProvider 生成 content URI。
  - 外部索引：使用原 SAF 持久 URI；若失效则复制到缓存目录分享。
- 多张分享时必要时降级为保存到系统相册，提供“保存到相册”备用方案。
- 缓存分享目录定时清理。

### 11. 其他功能

- 批量操作：长按多选，批量打标签、删除、导出、分享。
- 媒体详情页：大图/GIF/视频播放，展示信息（描述、标签、时间、大小、来源），可编辑，左右滑动切换。
- 设置界面：默认存储类型、分片大小、预设标签颜色管理、时间分组依据、回收站天数、主题（浅/深/跟随系统 + 自定义主题色）、缓存清理、数据库备份/恢复、关于。
- 性能保障：Paging 3 分页加载、Coil 异步缩略图、Room 索引优化、粘性时间头。

---

## 二、界面布局与技术栈

### 信息架构

单 Activity + 多 Fragment（Compose）导航，底部导航栏三项：
- **相册**：主媒体网格/列表，浏览、筛选、搜索
- **标签**：标签管理器
- **设置**：所有设置项

好的，我把第二步中所有用文字画的界面布局图全部整理出来，补充到文档的界面布局部分。

---

## 第二步：界面布局与技术栈（完整版）

### 一、整体信息架构
应用采用**单 Activity + 多 Fragment**导航结构，底部导航栏为主入口：

| 导航项   | 功能                              | 图标语义  |
| -------- | --------------------------------- | --------- |
| **相册** | 主媒体网格/列表，浏览、筛选、搜索 | 图片/网格 |
| **标签** | 标签管理器（增删改、排序）        | 标签/书签 |
| **设置** | 所有设置项                        | 齿轮      |

顶部工具栏根据页面动态变化。详情页、批量操作栏、回收站等通过 Fragment 导航或 Dialog/BottomSheet 承载。

---

### 二、屏幕布局（逐屏描述）

#### 1. 主相册界面
```
┌──────────────────────────────┐
│ 🔍 搜索栏（可收缩）     [🔽]│  ← 点击漏斗图标展开筛选面板
├──────────────────────────────┤
│ [全部] [😂开心] [🔥爆笑] …  │  ← 横向滑动胶囊标签筛选栏
│  ← 每个胶囊带标签背景色     │
├──────────────────────────────┤
│  ┌────────────────────────┐  │
│  │ 筛选面板（下拉展开）    │  │
│  │ 类型: [全部|图片|GIF|视频]│
│  │ 来源: [全部|相机|微信|…] │
│  │ 标签: [已选标签列表]    │  │
│  │ 描述: [不限|有|无]      │  │
│  │ 导出: [不限|已导出|未导出]│
│  │ 时间: [起始] - [截止]   │  │
│  │ 大小: [最小] - [最大]   │  │
│  │ [应用筛选] [清除全部]   │  │
│  └────────────────────────┘  │
├──────────────────────────────┤
│  网格视图（3列或可调）       │
│ ┌───┐ ┌───┐ ┌───┐          │
│ │   │ │   │ │   │          │
│ │ ●●│ │ ● │ │ ●●│ ← 缩略图 │
│ │   │ │   │ │ +1│   右下角  │
│ └───┘ └───┘ └───┘   彩色圆点│
│ ┌───┐ ┌───┐ ┌───┐          │
│ │   │ │   │ │   │          │
│ │   │ │ ●●│ │ ● │          │
│ └───┘ └───┘ └───┘          │
│  ... 按时间分组头(刚刚/昨天…)│
├──────────────────────────────┤
│  [底部导航栏: 相册|标签|设置]│
└──────────────────────────────┘
```
- 下拉显示筛选器快速面板（类型、来源、时间等快捷条件）。
- 长按缩略图进入多选模式，顶部变为批量操作栏（数量、打标签、导出、删除、分享图标）。
- 每个缩略图右下角彩色圆点规则：最多显示 4 个圆点，超出则末尾显示灰色 `+N`。圆点颜色为标签背景色，顺序按标签创建时间或名称排序（用户可在标签管理器调整）。

**时间分组头**：悬浮在网格上方，滚动时粘性停留（Sticky Header）。

---

#### 2. 批量操作栏（多选模式）
```
┌──────────────────────────────┐
│ ← 取消  已选 3 项  🏷️📤🗑️📤│  ← 批量操作图标
├──────────────────────────────┤
│  （网格视图，选中项高亮）     │
└──────────────────────────────┘
```
- 长按缩略图进入多选模式。
- 顶部操作栏：取消按钮、已选数量、打标签、导出、分享、删除图标按钮。

---

#### 3. 详情页
```
┌──────────────────────────────┐
│ ← 返回      编辑      ⋮ 菜单│
├──────────────────────────────┤
│                              │
│      大图 / GIF / 视频       │
│      (可缩放、拖动)          │
│                              │
├──────────────────────────────┤
│ 文件信息区（可折叠）         │
│ 描述：xxxxxxx                │
│ 标签：[😂开心] [🔥爆笑]     │
│ 来源：微信  时间：06-19 14:30│
│ 大小：2.3MB  分辨率：1080×… │
│ [编辑描述] [管理标签]       │
├──────────────────────────────┤
│  ← 上一张    分享    下一张→ │
└──────────────────────────────┘
```
- 左右滑动切换上下一张，视频/GIF 点击播放。
- 编辑描述和标签可内联编辑或在底部弹出 BottomSheet。
- 右上角菜单包含：保存到相册、设为收藏、查看原始信息等。

---

#### 4. 标签管理器界面
```
┌──────────────────────────────┐
│ 标签管理器          [新建]   │
├──────────────────────────────┤
│ 拖拽排序手柄  标签名  颜色块 │
│ ≡  😂开心    [🟠]   ✏️ 🗑   │
│ ≡  🔥爆笑    [🟡]   ✏️ 🗑   │
│ ≡  [已导出]  [🟢]   ✏️  🔒  │ ← 不可删除
│ ...                          │
│ (长按拖拽调整顺序)           │
└──────────────────────────────┘
```
- 新建标签：弹出 BottomSheet，输入名称，显示当前预设颜色预览（自动分配，可点选色盘更换）。
- 标签排序决定缩略图圆点显示顺序和筛选栏排列顺序。
- 保留标签 `[已导出]` 不可删除（显示 🔒 图标），但可编辑颜色样式。

---

#### 5. 搜索界面
```
┌──────────────────────────────┐
│ ← 返回  [搜索框............] │
├──────────────────────────────┤
│  搜索结果列表                │
│ ┌────┬──────────────────┐   │
│ │    │ 标题行（匹配加粗）│   │
│ │ 缩 │ 描述行（最多两行）│   │
│ │ 略 │ 描述第二行...     │   │
│ │ 图 │ 2.3MB  06-19     │   │
│ └────┴──────────────────┘   │
│ ┌────┬──────────────────┐   │
│ │    │ ...               │   │
│ └────┴──────────────────┘   │
└──────────────────────────────┘
```
- 结果列表：左边 48dp 缩略图，右边为文字区。
- 文字区结构：
  - 标题行（单行，14sp）
  - 描述行（最多两行，12sp，灰色）
  - 信息行（大小 + 日期，11sp，灰色）
- 匹配内容加粗高亮，智能截断显示省略号。

---

#### 6. 设置界面
```
┌──────────────────────────────┐
│ 设置                         │
├──────────────────────────────┤
│ 存储                         │
│   默认存储类型 ▸              │
│   分片大小上限 ▸              │
│   存储空间占用 ▸              │
├──────────────────────────────┤
│ 显示                         │
│   时间分组依据 ▸              │
│   主题 ▸                      │
│   网格列数 ▸                  │
├──────────────────────────────┤
│ 回收站                       │
│   保留天数 ▸                  │
│   立即清空回收站              │
├──────────────────────────────┤
│ 标签                         │
│   预设颜色管理 ▸              │
│   恢复默认颜色                │
├──────────────────────────────┤
│ 数据                         │
│   导出数据库                 │
│   从备份恢复                 │
│   同步JSON开关               │
├──────────────────────────────┤
│ 关于                         │
│   版本 1.0.0                 │
│   开源许可                   │
└──────────────────────────────┘
```
- 分组列表形式，每组之间有分隔线。
- 带有 `▸` 的条目点击进入子页面或弹出选择对话框。

---

#### 7. 导出进度对话框
```
┌──────────────────────────────┐
│ 正在导出...                  │
│                              │
│  ████████░░░░░░  8/12 分片   │
│  当前：分片_003.emp          │
│  已导出：67 张图片            │
│                              │
│        [后台运行] [取消]     │
└──────────────────────────────┘
```
- 导出时显示进度条、当前分片名称、已导出数量。
- 可切换到后台运行，完成后发送通知。

---

### 三、其他浮层与弹窗

#### 1. 添加媒体入口（底部菜单）
```
┌──────────────────────────────┐
│  选择图片/视频               │
│  选择文件                    │
│  导入整个文件夹              │
│  导入 .emp 分片              │
│  ─────────────────           │
│  取消                        │
└──────────────────────────────┘
```

#### 2. 添加编辑界面
```
┌──────────────────────────────┐
│ ← 取消  已选 3 项   [确认]   │
├──────────────────────────────┤
│ 缩略图列表（可滑动）         │
│ [图1] [图2] [图3]           │
├──────────────────────────────┤
│ 文件名：image_001.png        │
│ 标签：  [😂开心] [🔥爆笑]   │
│        [+添加标签]           │
│ 来源：  [微信 ▾]             │
│ 子目录选项：[包含子目录]     │
├──────────────────────────────┤
│ 批量应用到全部选择           │
└──────────────────────────────┘
```

#### 3. 分享菜单
```
┌──────────────────────────────┐
│  系统分享                    │
│  保存到系统相册              │
│  ─────────────────           │
│  取消                        │
└──────────────────────────────┘
```
- **系统分享**：弹出系统分享选择器。
- **保存到系统相册**：兜底方案，通过 `MediaStore` 保存后从相册发送。

---

### 四、底部导航栏结构
```
┌──────────────────────────────┐
│  [相册]  [标签]  [设置]      │
│   📁      🏷️      ⚙️        │
└──────────────────────────────┘
```
- 使用 Material3 `NavigationBar`，支持图标和文字。
- 当前选中项高亮，其他项灰色。

---

以上为第二步界面布局与技术栈的完整版，包含了所有之前讨论过的界面线框图。现在整个规划文档从第一步到第四步全部完整，可以作为后续开发的依据。

### 技术栈选择
| 层级 | 技术选型 |
|------|----------|
| 语言 | Kotlin 100% |
| UI 框架 | Jetpack Compose + Material3 |
| 导航 | Navigation Compose |
| 数据库 | Room + FTS5 |
| 图片加载 | Coil (GIF/视频缩略图支持) |
| 分页 | Paging 3 |
| 中文分词 | Jieba (用于 FTS 分词) |
| 文件操作 | Okio |
| 依赖注入 | Hilt |
| 异步 | Kotlin Coroutines + Flow |
| 设置存储 | DataStore (Preferences) |
| 后台任务 | WorkManager |
| 最低 API | 26 (Android 8.0) |

---

## 三、关键算法与处理逻辑

### 1. 标签预设颜色循环
- 预设颜色数组，新建标签时按标签当前最新颜色在数组中的下一个颜色分配，删除后不影响后续循环。

### 2. 缩略图圆点图例

- 取排序后的前4个标签，绘制实心圆；若总数>4，第4个后显示灰色“+N”。

### 3. 搜索结果展示与高亮截断算法（详细规则）

**目标**：在搜索结果列表中，将匹配关键词的内容以加粗高亮显示，并对超出可用宽度的文本进行智能截断，保证匹配部分始终可见。

**显示布局**：
- 左侧 48dp 缩略图
- 右侧文字区：
  - 标题行：单行，14sp，默认文字色
  - 描述行：最多两行，12sp，灰色 #666
  - 信息行：大小 + 日期，11sp，灰色 #999，格式如 `2.3 MB  06-19 14:30`

**高亮与截断规则**：

1. **查找匹配区间**  
   对搜索关键词 `query` 和原始文本 `text`（标题或描述）进行不区分大小写的全文匹配，定位所有匹配的起始索引。记录所有匹配区间 `[s, e]`。

2. **标题行处理（单行）**  
   设一行最大显示字符数 `MAX`（根据屏幕宽度动态计算，约 20-25 字）。
   - **情况 A**：`text.length ≤ MAX` → 直接显示全部，匹配区间加粗。
   - **情况 B**：需截断。优先保证所有匹配区间在显示窗口内。
     - **单匹配区间**：以该区间为中心，向左右扩展至总字符数 = `MAX`。左边不足则用右边补齐，反之亦然。两侧被截断处加省略号 `…`。
     - **多匹配区间且总跨度（最左到最右）≤ MAX**：窗口覆盖所有匹配，左右均匀扩展至 `MAX`，两端截断加省略号。
     - **多匹配区间且总跨度 > MAX**：选出最重要的匹配（如第一个匹配或最长匹配），按单匹配算法处理该匹配，其他匹配不可见。
     - **匹配内容本身超过 MAX**：仅显示该匹配内容片段，两端加省略号，片段内全部加粗。

3. **描述行处理（两行）**  
   - 首先按原文流顺序填充两行文本，优先保证第一行完整。
   - 若所有匹配区间能在两行内完整展示，则正常显示，尾部若需截断加省略号。
   - 若不能，则构造“相关片段”：取包含第一个匹配的上下文窗口（约 `2*MAX` 字符），尽量使匹配靠前，填充至两行，边缘截断加省略号。
   - 所有匹配区间加粗。

4. **信息行处理**  
   不参与高亮，直接显示 `文件大小 + 空格 + 日期`。

5. **性能优化**  
   文本截断和高亮索引的计算均在数据层（ViewModel）完成，生成 Compose 可用的 `AnnotatedString`，避免 UI 线程计算卡顿。

**示例**：  
搜索词 `猫`，标题 `搞笑猫咪表情包大全`，若 `MAX=10`，匹配区间 `[3,4]`。  
显示结果为 `…搞笑**猫**咪表情包…`。

### 4. 模糊搜索排序算法

**输入**：用户输入的关键词 `query`。

**处理流程**：
1. **分词**：使用 Jieba 分词库对 `query` 进行中文分词，得到词条列表 `terms`。
2. **FTS5 查询**：将 `terms` 拼接为 `MATCH 'term1 OR term2 OR ...'` 语句，从 Room FTS 虚拟表中查询，限制候选集 top 50。
3. **二次排序**：
   - 对每个候选媒体，分别计算其**文件名**和**描述**与原始 `query` 的 Levenshtein 编辑距离。
   - 文件名编辑距离权重设为 0.6，描述编辑距离权重设为 0.4。
   - 最终得分 = `FTS 相关性分数 × 0.7 + (1 / (加权编辑距离 + 1)) × 0.3`。
4. **返回结果**：按得分降序排列，取前 20 条返回 UI，避免一次渲染过多结果。

**性能保障**：编辑距离计算在协程中异步执行，不在 UI 线程阻塞。候选集已通过 FTS 初步过滤，避免对全表做编辑距离计算。

---

### 5. .emp 分片打包算法

**输入**：待导出媒体列表（已按时间降序排列）、分片大小上限 `MAX_SIZE`（字节，默认 100MB）。

**打包逻辑**：
1. 遍历媒体列表，维护当前分片状态：
   - `currentFiles: List<MediaEntity>`：当前分片内媒体列表
   - `usedSize: Long`：当前已用大小（初始值 = 预估元数据 JSON 长度 + 文件头大小，约 4KB）
2. 对每个媒体文件：
   - 获取文件大小 `fileSize`。
   - 计算加入后的大小：`newSize = usedSize + fileSize + 文件头开销（约 200 字节，含文件名长度、文件名、文件大小字段）`。
   - **条件 A**：`newSize ≤ MAX_SIZE` → 加入当前分片，更新 `usedSize = newSize`。
   - **条件 B**：当前分片为空且 `fileSize > MAX_SIZE`（超大单文件）→ 加入当前分片，即使超出上限。
   - **否则**：完成当前分片写入 `.emp` 文件，开启新分片，将该媒体加入新分片。
3. 分片写入 `.emp` 文件（字节流顺序）：
   | 偏移量 | 内容                                                         | 大小    |
   | ------ | ------------------------------------------------------------ | ------- |
   | 0      | 魔数 `0x454D4F4A` ("EMOJ")                                   | 4 字节  |
   | 4      | 版本号 `0x0001`                                              | 2 字节  |
   | 6      | 媒体数量（网络字节序）                                       | 4 字节  |
   | 10     | 元数据 JSON 字节长度                                         | 4 字节  |
   | 14     | 元数据 JSON（UTF-8 编码）                                    | 变长    |
   | 14+N   | 依次每个媒体文件：文件名长度(2B)+文件名(UTF-8)+文件内容长度(8B)+文件二进制内容 | 变长    |
   | 末尾   | 以上全部数据的 SHA-256 哈希                                  | 32 字节 |
4. 使用 Okio `BufferedSink` 流式写入，不将整个媒体文件加载到内存。

---

### 6. 导入校验与去重算法

**校验流程**：
1. 读取 `.emp` 文件最后 32 字节，作为原始哈希 `originalHash`。
2. 对文件除最后 32 字节外的全部内容计算 SHA-256，得到 `calculatedHash`。
3. 比对 `originalHash` 与 `calculatedHash`：
   - 一致 → 继续导入。
   - 不一致 → 提示用户“文件已损坏”，跳过该分片。

**提取流程**：
1. 读取元数据长度（偏移 10，4 字节），解析 JSON 获取媒体列表。
2. 依次读取每个媒体文件：
   - 读取文件名长度 → 读取文件名
   - 读取文件内容长度 → 读取文件二进制内容
   - 将文件写入公共目录（模式二存储）

**去重策略**：
- 导入前，根据当前设置的去重策略检查：
  - **路径严格**：目标路径已存在则跳过。
  - **文件名+大小**：同文件名且大小完全相同则跳过。
- 跳过时记录日志，不提示用户。

**标签处理**：
- 元数据中原有的 `[已导出]` 标签，在导入时**主动移除**，确保导入后的图片均处于未导出状态。
- 其余标签正常保留并合并到数据库。若标签名已存在则复用已有标签 ID，若不存在则创建新标签。

**错误处理**：
- 单个文件提取失败不影响同分片内其他文件。
- 提取完成后显示导入摘要：成功 X 张，跳过 Y 张，失败 Z 张。

---

### 7. 分享 URI 获取策略

根据媒体存储类型，依次尝试获取可分享的 URI：

**模式一（私有内部存储）与模式二（公共目录）**：
1. 通过 `FileProvider` 为该文件生成 `content://` URI。
2. 在分享 Intent 中设置 `FLAG_GRANT_READ_URI_PERMISSION`，授予接收方临时读取权限。
3. FileProvider 需在 `AndroidManifest.xml` 中配置对应路径：
   - 内部存储：`<files-path name="media" path="media/" />`
   - 公共目录：`<external-path name="public_media" path="EmojiManager/" />`

**模式三（索引外部目录）**：
1. 从数据库取出原始 SAF 持久 URI（`content://`）。
2. 尝试打开该 URI 的输入流（`contentResolver.openInputStream(uri)?.close()`）：
   - **成功** → 直接使用该 URI 构建分享 Intent。
   - **失败**（权限已撤销、文件已移动）→ 降级处理：将文件复制到应用缓存分享目录 `cache/share/`，生成唯一文件名（UUID + 原扩展名），通过 FileProvider 生成 URI 分享。
3. 分享完成后，由 WorkManager 定时任务清理缓存目录中超过 1 小时的文件。

**多张分享**：
- 使用 `Intent.ACTION_SEND_MULTIPLE`，传递 `ArrayList<Uri>`。
- 若部分图片需要复制，则全部降级为复制到缓存目录统一分享，避免混合 URI 权限出错。
- 若系统不支持多张分享（无应用响应），自动降级为“保存到系统相册”。

**兜底方案**：
- 分享按钮长按或下拉菜单提供“保存到系统相册”选项。
- 使用 `MediaStore` 插入文件到系统相册，完成后弹出 Toast 引导用户从相册发送。

---

### 8. 回收站清理算法

**触发方式**：WorkManager 定期任务（每 12 小时执行一次，DeviceIdle 约束，电量充足时运行）。

**清理逻辑**：
1. 从设置中读取回收站保留天数 `retentionDays`。
   - 若 `retentionDays == 0`，跳过清理（用户关闭了回收站功能，删除即永久删除）。
2. 计算截止时间：`cutoffTime = System.currentTimeMillis() - retentionDays * 24 * 3600 * 1000`。
3. 查询 Room 中满足条件的记录：
   ```sql
   SELECT * FROM media WHERE isDeleted = 1 AND deletedTime < :cutoffTime
   ```
4. 对每条记录执行永久删除：
   - **模式一/二**：删除对应存储目录中的文件。
   - **模式三**：仅删除数据库记录，不删除原文件。
   - 删除 `media_tag_cross` 中的关联记录。
   - 删除 FTS 索引条目。
5. 记录清理数量，写入日志。

**手动清理**：
- 用户在设置界面点击“立即清空回收站”时，直接执行上述清理逻辑（`retentionDays = 0` 特殊处理为立即全部清理）。
- 执行前弹窗确认，显示将被永久删除的媒体数量。

## 四、主逻辑与状态管理

### MVVM + Repository 架构
- UI 层消费 ViewModel 的 `StateFlow<UiState>`，发送事件。
- ViewModel 处理业务逻辑，调用 Repository。
- Repository 统一管理数据库与文件操作。

### 核心实体
- `MediaEntity`：id, fileName, filePath, storageType, mimeType, source, description, takenTime, importTime, fileSize, width/height, isDeleted, deletedTime, exportedHash 等。
- `TagEntity`：id, name, bgColor, isReserved, sortOrder。
- `MediaTagCrossRef`：mediaId, tagId。
- FTS 虚拟表：fileName, description。

### 关键业务流程
- **导入/添加**：选择文件 → 编辑界面（重命名、标签、来源）→ 根据存储类型复制/索引 → 写入 Room → 生成缩略图 → 更新 FTS。
- **相册浏览**：Paging 3 从 Room 查询，分组为 HeaderItem + MediaItem，使用 LazyVerticalGrid + stickyHeader 渲染。
- **搜索**：用户输入（防抖300ms） → 分词 → FTS MATCH → 编辑距离排序 → 生成高亮 AnnotatedString → 列表展示。
- **导出**：获取待导出列表 → 贪心打包 .emp → 进度通知 → 成功添加 `[已导出]` 标签。
- **导入**：选取 .emp → 校验哈希 → 解析元数据 → 提取文件到公共目录 → 写入 Room（去除导出标签）→ 刷新。
- **分享**：收集媒体 → 根据存储类型获取 URI → 构建 `ACTION_SEND` → 降级策略。

### 全局状态
```kotlin
data class AlbumUiState(
    val mediaList: LazyPagingItems<MediaWithTags>,
    val selectedMediaIds: Set<Long>,
    val isMultiSelectMode: Boolean,
    val activeFilters: FilterState,
    val searchQuery: String,
    val isSearchActive: Boolean,
    val exportProgress: ExportProgress?
)
```

### 设置持久化
使用 DataStore 存储：默认存储类型、分片大小、时间分组依据、回收站天数、主题模式/主题色、JSON同步开关、预设标签颜色列表等。

### 边界与异常处理
- 文件系统不可写 → 引导授权。
- 大文件播放 → ExoPlayer + 预加载限制。
- 磁盘空间不足 → 导出前预检查。
- 哈希校验失败 → 提示跳过。
- 搜索无结果 → 空状态占位。

---

*文档最后更新于项目基础架构搭建完成后，作为后续开发依据。*

---

## 五、基础架构搭建记录（已完成）

### 2026-06-22 环境配置与基础架构

#### 1. 依赖版本更新

全部第三方库已更新至当前最新稳定版，通过 Sync 验证。

| 库 | 版本 | 类型 |
|---|---|---|
| AGP | 9.2.1 | 最新稳定（保留原版本）|
| Kotlin | 2.2.10 | 最新稳定（保留原版本）|
| Gradle | 9.4.1 | 最新稳定（保留原版本）|
| KSP | 2.3.9 | 新增 — Room/Hilt 注解处理 |
| Compose BOM | 2026.06.00 | 升级（原 2026.02.01）|
| core-ktx | 1.19.0 | 升级（原 1.10.1）|
| lifecycle-runtime-ktx | 2.11.0 | 升级（原 2.6.1）|
| activity-compose | 1.13.0 | 升级（原 1.8.0）|
| Navigation Compose | 2.9.8 | 新增 |
| Room | 2.8.4 | 新增 |
| Paging Compose | 3.5.0 | 新增 |
| Coil + Coil Compose | 2.7.0 | 新增 |
| Hilt + Hilt Compiler | 2.59.2 | 新增 |
| Hilt Navigation Compose | 1.3.0 | 新增 |
| DataStore Preferences | 1.2.1 | 新增 |
| Okio | 3.17.0 | 新增 |
| WorkManager | 2.11.2 | 新增 |
| AndroidX JUnit ext | 1.3.0 | 升级（原 1.1.5）|
| espresso-core | 3.7.0 | 升级（原 3.5.1）|
| JUnit | 4.13.2 | 保留不变 |

兼容性验证：
- 所有依赖版本均由 Google Maven / Maven Central 的 `maven-metadata.xml` 确认为最新 `<release>` 版本，无 alpha/beta/rc 后缀。
- Kotlin 2.2.10 + Compose Compiler Extension（`org.jetbrains.kotlin.plugin.compose`）与 Compose BOM 2026.06.00 兼容。
- AGP 9.2.1 是截至查询时的最新稳定版（9.3.x 仅 RC，9.4.x 仅 alpha）。

#### 2. Hilt 依赖注入

创建文件：
- **`MemeManagerApp.kt`**：`@HiltAndroidApp` 注解的 Application 类。
- **`AndroidManifest.xml`**：`android:name=".MemeManagerApp"` 注册。
- **`di/DatabaseModule.kt`**：`@Module @InstallIn(SingletonComponent::class)`，提供 `AppDatabase`、`MediaDao`、`TagDao` 三个 `@Provides @Singleton` 实例。

#### 3. Room 数据库结构

`data/local/` 包下：

- **Entity**：
  - `MediaEntity`（表名 `media`）：id, name, filePath, type（MediaType 枚举：IMAGE/VIDEO/GIF）, size, width, height, createdAt, updatedAt。
  - `TagEntity`（表名 `tags`，name 唯一索引）：id, name。
  - `MediaTagCrossRef`（表名 `media_tag_cross_ref`，双主键 + 双外键 CASCADE 删除）：mediaId, tagId。
- **DAO**：
  - `MediaDao`：insert/update/delete/deleteById，getById(Flow)、getAll(Flow)、getByType(Flow)。
  - `TagDao`：insert/update/delete，getById(Flow)、getAll(Flow)、search(Flow)、getByName(suspend)。
- **Database**：
  - `AppDatabase`（version=1, exportSchema=false），注册 3 实体 + 2 DAO。

#### 4. 导航框架

- **`MainActivity.kt`**：添加 `@AndroidEntryPoint`，使用 `rememberNavController()` + `NavHost`。
- **三个路由**：
  | 路由 | 标签 | 图标 | Composable |
  |------|------|------|------------|
  | `album` | 相册 | `Icons.Filled.Home` | `AlbumScreen` |
  | `tags` | 标签 | `Icons.Filled.Star` | `TagsScreen` |
  | `settings` | 设置 | `Icons.Filled.Settings` | `SettingsScreen` |
- **底部导航栏**：`NavigationBar` + `NavigationBarItem`，图标来自 `material-icons-core`。
- **`ui/screen/Screens.kt`**：三个占位 Composable，居中显示标题文字。

#### 5. 当前项目结构

```
app/src/main/java/com/mememanager/
├── MemeManagerApp.kt              # @HiltAndroidApp
├── MainActivity.kt                # @AndroidEntryPoint + NavHost + 底部导航
├── di/
│   └── DatabaseModule.kt          # Hilt Module
├── data/local/
│   ├── AppDatabase.kt             # Room Database
│   ├── dao/
│   │   ├── MediaDao.kt
│   │   └── TagDao.kt
│   └── entity/
│       ├── MediaEntity.kt
│       ├── TagEntity.kt
│       └── MediaTagCrossRef.kt
└── ui/
    ├── screen/
    │   └── Screens.kt             # 占位页面
    └── theme/
        ├── Color.kt
        ├── Theme.kt
        └── Type.kt
```

项目已通过 AS Sync，可在此基础上按 `goals.md` 第四节的 MVVM + Repository 架构继续开发。实体设计已与规划文档中的核心实体保持对应（后续可按需补充 FTS 虚拟表、source/description/exportedHash 等字段）。